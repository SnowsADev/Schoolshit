

let ModuleTemplate = (function ($) {
    var configMap = null;

    //initialize function
    var initModule = function () {
        return true;
    }

    return {
        initModule: initModule
    };
})(jQuery);